machine:
  node:
    version: 6.2.2
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t circleci/testcontainer ./docker/test
deployment:
  staging:
    branch: master
    heroku:
      appname: guarded-reaches-48742
